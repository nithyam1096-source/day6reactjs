import React from "react";

function ListRenderingIntro() {
  return (
    <div>
      <h3>List Rendering Introduction</h3>
      <p>
        In React, lists are rendered using the <strong>map()</strong> method. The map()
        function iterates over an array and returns a new array of JSX elements
        to display in the UI.
      </p>
    </div>
  );
}

export default ListRenderingIntro;