// components/NoteOnProps.jsx
import React from "react";

export default function NoteOnProps() {
  return (
    <div>
        
      <h2>What are Props?</h2>
      <p>
        Props (short for properties) are used to pass data from one component to another
        in React. They make components reusable and dynamic by allowing us to send
        different values each time a component is used.
      </p>
    </div>
  );
}