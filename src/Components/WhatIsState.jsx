function WhatIsState() {
  return (
    <div>
      <h2>What is State?</h2>
      <p>
        State in React is a built-in object used to store dynamic data in a component.
        When the state changes, React automatically re-renders the component. 
        It is used to make components interactive and update UI based on data changes.
      </p>
    </div>
  );
}

export default WhatIsState;